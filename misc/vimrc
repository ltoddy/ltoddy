" 开启语法高亮
syntax on

" 左下角显示当前Vim模式
set showmode

" 支持使用鼠标
set mouse=a

" 使用utf-8编码
set encoding=utf-8

" 使用256色
set t_Co=256

"" 针对不同的文件,采取不同的缩进方式, e.g. .py文件会寻找Python缩进规则:~/.vim/indent/python.vim
filetype indent on

" 设置缩进
set autoindent

" 设置tab宽度
" set tabstop=4

"" 设置自动对齐空格数量
set shiftwidth=4

" 自动将tab转换为空格
set expandtab
set softtabstop=4

" 显示行号
set number

" 其他行都为相对于该行的相对行号
" set relativenumber

" 高亮当前行
set cursorline

" 设置行宽
set textwidth=120

" 自动折行
set wrap

" 垂直滚动时,光标距离顶部/底部的位置
set scrolloff=5

" 显示状态栏
set laststatus=2

" 状态栏显示光标的当前位置
set ruler

" 光标遇到括号,高亮对应的另一个
set showmatch

" 搜素时,高亮显示匹配结果
set hlsearch

" 在搜索模式时,每输入一个字符,就自动跳到第一个匹配的地方
set incsearch

" 搜索时忽略大小写
set ignorecase
set smartcase

" 不创建备份
set nobackup

" 不创建交换文件
set noswapfile

" 保留撤销历史
" set undofile

" 自动切换目录
set autochdir

" 出错时,不要发出响声
set noerrorbells

" 打开文件监视, 在编辑过程中文件发生外部变化(比如被其他文件编辑),就会发出提示
set autoread

" 命令模式下,底部操作指令按下tab键自动补全,第一次按下tab,会显示所有匹配的操作指令的清单,第二次按下tab会依次选择各个指令
set wildmenu
set wildmode=longest:list,full


